#ifndef LINALG_H
#define LINALG_H

double** dinamic_array(double** A, int m, int n); //function to create a random array dinamically in runtime
void print(double** A, int m, int n); //function to print the computed arrays/matrices

#endif